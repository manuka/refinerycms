<!DOCTYPE html>
<%= render '/refinery/html_tag' %>
  <% content_for :meta, tag(:meta, name: 'refinerycms', content: Refinery.version) %>
  <%= render 'refinery/admin/head' %>
  <body class="<%= I18n.locale %>">
    <%= render 'refinery/admin/top_nav' %>
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-3">
          <%= render 'refinery/admin/side_nav' %>
        </div>
        <div class="col-sm-9">
          <%= yield %>
        </div>
      </div>
    </div>
</html>
